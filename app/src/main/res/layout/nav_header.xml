<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="250dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/clg_image"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:paddingLeft="16dp">

    <ImageView
        android:id="@+id/nav_had_imageView"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="20dp"
        android:src="@drawable/ic_person"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:tint="@color/shrine_pink_100"
        />

    <TextView
        android:id="@+id/nav_stud_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Harshit Goshwami"
        android:textColor="@color/shrine_pink_100"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="#88000000"
        app:layout_constraintBottom_toBottomOf="@+id/nav_had_imageView"
        app:layout_constraintStart_toEndOf="@+id/nav_had_imageView"
        app:layout_constraintTop_toTopOf="@+id/nav_had_imageView" />
</androidx.constraintlayout.widget.ConstraintLayout>